package Lect_27_Sorting;

public class Insertion_Sort {
    public static void main(String[] args) {

        /*
        Insertion Sort
        this algo is adaptive by default
        this algo is stable as well
        this algo is having Time Complexity -:
        Best Case - O(N)
        Worst Case - O(N^2)
        this algo run till arrays.length-1 times.
        this algo is best used in linked list it is not made for array but can  be use for it no issue
        this algo main point is to shift the bigger element by comparing from the current element that you have in i.
        for(int i =1; i<nums.length;i++){
        int x =nums[i];
        int j  =i-1;
        while(j >-1 && nums[j] > x){
        nums[j +1] =nums[j];
        j--;
        }
        nums[j+1] =x;
        }

         */
    }
}
